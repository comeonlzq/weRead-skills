# 阶段四：HTML 报告生成（visionOS 玻璃态风格）

读取 [03-analyze.md](./03-analyze.md) 的结构化分析结果，生成单文件 HTML 报告。

## 输出要求

- 文件：工作目录 `weread_report.html`，单文件，**零代码依赖**（不引 CDN/字体/图标库，图表用纯 CSS/SVG 实现），离线可打开。
- **唯一允许的外部资源**是接口返回的书籍封面图片 URL（直接 `<img>` 引用，使用规范见下）。
- 生成后告知用户文件路径，并提示可直接双击在浏览器打开。

## 设计规范：Apple Vision Pro 风格

核心设计语言：**景深（Depth）+ 玻璃态（Glass）+ 光影（Light）**。页面是"空间"，卡片是悬浮在空间中的玻璃面板。

### 1. 空间背景层（Depth 的来源）

```css
body {
  background: #05070E; /* 深空底色 */
}
/* 2-3 个彩色光斑（glow orbs），用固定定位 div + radial-gradient 实现，
   蓝色为主（呼应主题色），错落分布在页面四角，营造空间纵深感 */
.orb {
  position: fixed; border-radius: 50%; filter: blur(120px); z-index: 0;
}
/* 例：左上 #1B88EE（透明度 0.25）、右下 #0F5FCC（0.2）、中部点缀青色 #38BDF8（0.12） */
```

### 2. 玻璃卡片层（Glass）

```css
.glass-card {
  position: relative; z-index: 1;
  background: rgba(255, 255, 255, 0.06);          /* 半透明白 6%-12% */
  -webkit-backdrop-filter: blur(24px) saturate(160%);
  backdrop-filter: blur(24px) saturate(160%);      /* 磨砂玻璃核心 */
  border: 1px solid rgba(255, 255, 255, 0.12);     /* 细亮边框 */
  border-radius: 24px;                             /* 大圆角，visionOS 特征 */
  box-shadow:
    0 8px 32px rgba(0, 0, 0, 0.35),                /* 悬浮投影 */
    inset 0 1px 0 rgba(255, 255, 255, 0.22);       /* 顶部高光：模拟光源在左上方的镜面反射 */
}
```

- **景深分层**：光斑层（z:0）→ 玻璃卡片层（z:1）→ 卡片内容（z:2）；Hero 区的核心大数字可再套一层更亮的玻璃小面板（背景 0.12 白、圆角 16px），形成"悬浮玻璃片"效果。
- **光影方向统一**：光源固定左上方——顶部高光朝上、投影朝下偏右，全页一致。
- **性能红线**：`backdrop-filter` 元素全页控制在 30 个以内；列表项不要逐条套玻璃，整块内容用一张玻璃卡片承载。
- **降级**：`@supports not (backdrop-filter: blur(1px))` 时，卡片背景改为更实的 `rgba(22, 30, 48, 0.92)`，保证可读性。
- **克制原则**（Liquid Glass 规范）：玻璃材质用于卡片容器和章节导航/徽章等控件层；正文内容直接排在卡片内，**不要**给文字再加玻璃层。

### 3. 色彩与文字（Light 的落点）

| 项 | 要求 |
|----|------|
| 主色 | 科技蓝 `#1B88EE`（渐变辅助 `#1B88EE → #0F5FCC`） |
| 强调 | 暖橙 `#FF9F43`（用于次级强调数字，如环比增长） |
| 标题文字 | `#FFFFFF`，关键数字 48px 加粗，可加蓝色泛光 `text-shadow: 0 0 24px rgba(27,136,238,.5)` |
| 正文文字 | `rgba(235, 240, 250, 0.78)`；弱化说明 `rgba(235, 240, 250, 0.5)` |
| 对比度 | 全部文字对背景对比度 ≥ 4.5:1（玻璃卡片上的文字尤其注意） |
| 字体 | `system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`（近似 visionOS 的 SF 观感）；标题 letter-spacing `-0.02em` |
| 时长格式 | 一律"X 小时 Y 分钟" |

### 4. 图表（深色空间适配）

| 图表 | 实现方式 |
|------|----------|
| 历年/月度趋势 | flex 柱状图：柱体 `linear-gradient(180deg, #4FA8FF, #1B88EE)` + 底部发光 `box-shadow: 0 0 16px rgba(27,136,238,.4)`；悬停 `title` 显示"X年 · Y小时" |
| 24 小时热力条 | 24 格，透明度随数值从 `rgba(27,136,238,.08)` 到 `#4FA8FF` 渐进，代表"点亮的空间" |
| 分类偏好 | 横向条形图，玻璃轨道（`rgba(255,255,255,.08)`）+ 蓝色渐变填充 |
| 读听比例 | SVG 环形图（`stroke-dasharray`），`#1B88EE` 主环 + `rgba(255,255,255,.15)` 底环 |

条形宽度按数值占最大值百分比内联计算。

### 4.5 书籍封面图片

书榜、最近在读等书籍列表**直接引用接口返回的 `cover` URL**（见 02-collect.md 数据口径）：

```css
.book-cover {
  width: 56px; height: 78px;           /* 固定比例约 5:7，列表场景可等比缩小 */
  object-fit: cover; border-radius: 8px;
  border: 1px solid rgba(255, 255, 255, 0.15);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4);  /* 与玻璃卡片同光源：左上亮、右下影 */
}
```

- `<img loading="lazy">` 惰性加载；`alt` 填书名。
- **必须有降级**：`onerror` 时把 `<img>` 替换为纯 CSS 占位封面（渐变底色 `linear-gradient(135deg, #1B88EE, #0F5FCC)` + 居中显示书名首字或前两字），禁止出现破图图标。
- 无 `cover` 字段的书直接渲染占位封面，不留空白。
- 同一列表内封面尺寸必须统一，与右侧文字基线对齐。

### 5. 交互与动效（克制）

- 卡片 hover：`transform: translateY(-4px)` + 投影加深，`transition: 0.3s ease`；仅桌面端生效。
- **必须**加入 `@media (prefers-reduced-motion: reduce)` 关闭所有位移/发光过渡。
- 禁止大面积渐变横幅、粒子、滚动视差等动效堆砌；emoji 只点缀章节标题。

### 6. 布局与响应式

- 居中最大宽度 860px；断点 375 / 768 / 1024 / 1440px 均需检查；`max-width: 640px` 时降为单列、卡片圆角减至 18px、核心数字缩小到 36px。
- 文本禁止截断：标签、徽章、引文必须完整换行重排，不溢出不裁切。

## 页面结构（自上而下）

```
1. Hero 头部：阅读人格称号（玻璃大字徽章）+ 年度之句开篇引言（用户自己的划线原文，注明书名与时间）+ 报告标题 + 统计范围 + 趣味标签（玻璃胶囊）+ 三个核心大数字（悬浮玻璃片）
2. 历年阅读趋势：多年柱状图（悬停显示"X年 · Y小时"）+ 偏好迁移叙事（按 03 写作规范撰写，非干巴巴的数字罗列）
3. 阅读作息：24 小时热力条 + 时段人格标签 + 黄金阅读时段文字（用意象化语言描述，如"23 点后的灯"）
4. 口味画像：分类偏好 Top5 条形图 + 读听比例环形图（如有），配一句口味白描
5. 笔记洞察：3-5 个核心问题卡片，每卡含问题标题 + 1-2 条真实划线/想法引用（注明书名）+ 一段"解读→照见"文字 + 笔记风格标签 + 想法/划线比
6. 人物画像与动物人格：画像白描（150 字，含场景细节）+ 动物人格大徽章（`emoji + 动物名 + 注解`，玻璃悬浮片，emoji 放大展示）+ 数据依据列表 + "娱乐性推测"角标
7. 知识版图：左右两栏对照——优势领域（蓝色高亮 + 积累深度 + 一句"老农式"赞叹）vs 盲区领域（暗色虚线框 + 温柔的轻建议）
8. 年度书榜与最爱作者：读书时长 Top10 列表（每行含封面图 + 书名 + 时长右对齐，含 tags）+ 作者卡片
9. 书架体检：书架总数 / 读完率 / 囤积指数 / 私密书 / 最近在读 5 本（封面图 + 书名 + 进度条；囤积、未读完用理解和温柔口吻）
10. 结语：3-5 句第二人称个性化总结，完整走"现象 → 解读 → 照见"三层结构，以一句开放式的祝愿收束；底部注明数据来源与生成日期
```

## 质量红线

1. **禁止编造**：每个数字都能追溯到接口字段；缺失模块整块省略或显示"暂无数据"。
2. 时间戳一律 `YYYY-MM-DD`；时长一律"X 小时 Y 分钟"；百分比保留整数。
3. 报告文案风格按阶段一用户选择：客观型 = 数据准确、措辞克制（人称可用"你"，但去掉抒情与意象）；趣味型 = 完整执行 03-analyze.md 的洞察写作规范（第二人称、意象、三层结构）。
4. 长文案（结语、标签说明）必须基于已计算的真实数据。
5. **笔记引用必须逐字来自接口返回**（`markText`/`content`），不得改写润色；核心问题提炼可换表述，但证据原文不得改动。
6. **人物画像与动物人格属于推测**：必须标注"娱乐性推测"，每条论断附数据依据；动物选择必须与数据组合匹配；证据不足时给"1 主 1 副"两只动物并说明分歧，禁止绝对化断言（"你就是 XX 动物"）。
7. **盲区判定必须基于数据**：只列分类时长为零或极低的领域，禁止凭"大家都不读"之类常识推断；盲区推荐书目必须来自接口真实返回。
8. **洞察文案必须过 03-analyze.md 写作规范自检**：每个洞察"证据 + 解读"成对出现；禁止空洞夸奖与 AI 腔套话；句式模板不得照抄（替换为真实数据后同一模板全篇最多一次）；年度之句与笔记引用逐字来自接口数据。
9. **敏感内容过滤**：所有展示在报告中的笔记原文（年度之句、笔记洞察引用、Hero 引言）必须已过 03-analyze.md 的"敏感内容过滤"；涉及健康/心理困境、亲密关系、职场财务隐私、强烈负面宣泄的内容一律不得作为引用展示，画像类文案也不得透露可识别的私密细节。

## 交付前检查清单

- [ ] 所有玻璃卡片文字对比度 ≥ 4.5:1
- [ ] `backdrop-filter` 不支持时有实底降级
- [ ] `prefers-reduced-motion` 已处理
- [ ] 375 / 768 / 1024 / 1440px 宽度下无溢出、无截断
- [ ] 图表条形宽度与数据成比例（抽查 2 项核对）
- [ ] 封面图引用了 `cover` URL，且 `onerror` 降级占位封面已实现（无破图）
- [ ] 全文搜索确认无"undefined""NaN"“null”残留
