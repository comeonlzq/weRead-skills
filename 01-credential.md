# 阶段一：凭据与范围确认

## 1. 获取 API Key

- 若环境变量 `WEREAD_API_KEY` 已设置（格式 `wrk-xxxxxxxx`），直接使用。
- 否则**必须先询问用户**："请提供你的微信读书 API Key（格式 wrk-xxxx）"。
- 拿到 Key 后先发一个轻量请求验证有效性（如 `/user/notebooks`, count=1），失败则提示用户检查 Key。

## 2. 确认报告范围

询问用户（一次问完，避免多轮打断）：

1. **统计范围**：本年 / 总计（overall）/ 指定年份（如"2025 年度报告"）。默认"本年 + 总览"。
2. **报告语言风格**：客观图表型 / 带个性化趣味文案型。默认后者。

用户未回答时使用默认值，不要反复追问。

## 完成标志

- ✅ 拿到有效 API Key（校验请求成功）
- ✅ 已确认统计范围与文案风格（或采用默认值）

完成后向用户汇报一句话，然后进入 [02-collect.md](./02-collect.md)。
